Convolutional Neural Network
Type of deep learning model that is good at processing spatial data such as video, image, audio spectrograms

Convolution operation is the heart of CNN. It uses a small filter matrix that moves across the image computing weighted sums of local pixel region;