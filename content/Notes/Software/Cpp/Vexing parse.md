The idea that a function would ambiguously be otherwise considered an object declaration would instead be interpreted by the compiler as a function

[[Brace initialization]] is a way that you can avoid misinterpreting an object as function

You can also just omit parenthesis which is what you are probably used to. This is how variables are usually defined.